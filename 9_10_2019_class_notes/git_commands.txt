git status #tells us which files ro push up

git add -A #Adds all the files to be saved

git commit -m "Description of what changes" # Describes how rthe files were changed

git push origin master #Saves the files to git

git checkout -b [branch_name] #creates a new branch

git branch #tells us branch we are in

git checkout [branch_name] #switch between branches

git pull origin [branch_name] #pull the latest changes
